<nav class="navbar navbar-expand-lg navbar-transparent navbar-absolute fixed-top ">
    <div class="container-fluid">
        <div class="navbar-wrapper">
            <div class="navbar-minimize">
                <button id="minimizeSidebar" class="btn btn-just-icon btn-white btn-fab btn-round">
                    <i class="material-icons text_align-center visible-on-sidebar-regular">more_vert</i>
                    <i class="material-icons design_bullet-list-67 visible-on-sidebar-mini">view_list</i>
                </button>
            </div>

            @if (ViewContext.RouteData.Values["controller"].ToString() == "OpdExpense")
            {
                <a class="navbar-brand" href="javascript:;">OPD Expense</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "HospitalExpense")
            {
                <a class="navbar-brand" href="javascript:;">Hospital Expense</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "TravelExpense")
            {
                <a class="navbar-brand" href="javascript:;">Employee Expense</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "FinApproval")
            {
                <a class="navbar-brand" href="javascript:;">Finance Approval</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "ManApproval")
            {
                <a class="navbar-brand" href="javascript:;">Manage Approval</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "HrApproval")
            {
                <a class="navbar-brand" href="javascript:;">Human Resource Approval</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "HospitalExpense")
            {
                <a class="navbar-brand" href="javascript:;">Expense</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "OpdExpenseImage")
            {
                <a class="navbar-brand" href="javascript:;">Expenses Image</a>
            }
            @if (ViewContext.RouteData.Values["controller"].ToString() == "OPDExpensePatient")
            {
                <a class="navbar-brand" href="javascript:;">Patient List </a>
            }
        </div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
            <span class="sr-only">Toggle navigation</span>
            <span class="navbar-toggler-icon icon-bar"></span>
            <span class="navbar-toggler-icon icon-bar"></span>
            <span class="navbar-toggler-icon icon-bar"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link" href="javascript:;" id="navbarDropdownProfile" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="material-icons">person</i>
                        <p class="d-lg-none d-md-block">
                            Account
                        </p>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownProfile">
                        @if (Request.IsAuthenticated)
                        {
                            <a href="@Url.Action("SignOut", "OfficeManager", null, Request.Url.Scheme)" class="dropdown-item">Log out</a>
                        }
                        else
                        {
                            <a href="@Url.Action("SignIn", "OfficeManager", null, Request.Url.Scheme)" class="dropdown-item">Login</a>
                        }
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

